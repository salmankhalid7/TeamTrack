// src/components/features/admin/quizzes/QuestionEditor.jsx
// This can be used to add/edit a single question – we'll keep it simple.
// For now, we'll just use it as a placeholder.
export function QuestionEditor({ question, onSave, onCancel }) {
  // Implementation similar to the pool editor but for one question.
  // I'll skip full code for brevity – you can later enhance.
  return <div>Question Editor</div>;
}